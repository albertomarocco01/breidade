# components/gate

Empty for now — **filled in Prompt 2**.

Home for the GATE experience: the handwritten reveal, the two "doors" opening
into Grapholio / Fotofolio, the subtle bilingual 設計師 flourish, and the WebGL
accent (via the `components/canvas/FieldMount` seam). The current gate placeholder
lives in `src/app/page.tsx`.
