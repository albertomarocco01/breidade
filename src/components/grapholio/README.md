# components/grapholio

Empty for now — **filled in Prompt 2**.

Home for the Grapholio components: the bold project grid, handwritten project
titles (`--font-hand`), per-project accent treatments, cover/gallery imagery
(`next/image`), and the single-project case study. The current scaffold lives in
`src/app/grapholio/`.
